<div class="row">
  <div class="col-xs-12">
    {{#if model.length}}
    <ul class="list-unstyled dashboard-list">
      {{#each model as |item|}}
      <li>
        <div class="row">
          <div class="col-xs-12">

            <div class="row">
              <div class="col-xs-10">
                {{publication-list-row item=item link='publications.dashboard.manage.show' showMetaData=false}}
              </div>
            </div>
            <div class="row">
              <div class="col-xs-12">
                {{review-item item=item setMsgHeader='showMessage' refreshReviewCount='refreshReviewCount' editItem='editItem'}}
              </div>
            </div>

          </div>

        </div>
      </li>
      {{/each}}
    </ul>
    {{else}}
    {{t 'publications.dashboard.review.youHaveNoPublicationsForReview'}}
    {{/if}}
  </div>
</div>
