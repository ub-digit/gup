<div class="container-fluid">
	<div class="row">
		<div class="col-xs-12">
			{{#link-to 'publications.manage.index' class="btn btn-default"}}<i class="fa fa-chevron-left"></i> Tillbaka{{/link-to}}
		</div>
		<div class="col-xs-6">
			<div class="row">
				<div class="col-xs-12">
					{{display-publication publication=model}}
				</div>
			</div>
		</div>
		<div class="col-xs-2">
			<div id="publication-toolbar" class="col-xs-12">
					{{#link-to  'publications.manage.show.edit' model class='btn btn-primary'}}Redigera{{/link-to}}
					{{#if model.is_draft}}

					{{/if}}
			</div>
		</div>
	</div>
</div>