<div class="row">
  <div class="col-xs-12">
          {{#if model.length}}
          <ul class="list-unstyled dashboard-list">
            {{#each item in model}} 
              <li>
                <div class="row">
                  <div class="col-xs-10">
                    {{#if item.title}}
                      <div class="row">
                        <div class="col-xs-12">
                        	<div class="dashboard-list-header">{{#link-to 'publications.manage.show' item.pubid }}{{item.title}}{{/link-to}}</div>
                        </div>
                      </div>
                      <div class="row">
                        <div class="col-xs-12"><strong>Författare:</strong> {{item.author}}</div>
                      </div>
                      
                    {{else}}
                      <div class="dashboard-list-header">{{#link-to 'publications.manage.show' item.pubid }}Titel ej angiven{{/link-to}}</div>
                    {{/if}}
                  </div>
                  <div class="col-xs-2">
                    <div class="btn-toolbar pull-right">
                        {{#link-to  'publications.manage.show' item.pubid classNames="btn btn-primary btn-sm"}}Visa{{/link-to}}
                    </div>
                  </div>
                </div>
              </li>
            {{/each}}
          </ul>
          {{else}}    
            Du har inga publikationer.
          {{/if}}
  </div>
</div>