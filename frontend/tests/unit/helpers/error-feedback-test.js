import {
  errorFeedback
} from '../../../helpers/error-feedback';
import { module, test } from 'qunit';

module('ErrorFeedbackHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = errorFeedback(42);
  assert.ok(result);
});
