import {
  translateKey
} from '../../../helpers/translate-key';
import { module, test } from 'qunit';

module('TranslateKeyHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = translateKey(42);
  assert.ok(result);
});
