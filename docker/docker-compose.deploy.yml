version: '3.7'
services:
  frontend:
    ports:
      - ${GUP_FRONTEND_PORT}:8080
  db:
    deploy:
      placement:
        constraints: [node.role == manager]
networks:
  backend:
    driver: overlay
    attachable: true

