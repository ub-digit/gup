---
# Docker container internal ports
ports:
  gup_solr_port: 8983
  gup_frontend_port: 8080 # Pass to container? Only needed if running dev container in nomad
  gup_rails_port: 3000
  gup_postgres_port: 5432
